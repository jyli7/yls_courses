<% unless line_item.course.blank? %>
	<% if line_item == @current_item %>
		<div id = "current_item">
		<% else %>
		<div class = "non_current_item">
	<% end %>	
			<div class="delete_line_item"><%= link_to 'Remove', line_item, {:method => :delete, :remote => true} %></div>
			<li class="name_body"><%= line_item.course.name %></li>
			<li class="units_body_cart"><%= line_item.course.units_alt %></li>
			<br>
		</div>
<% end %>